{
  "name": "@telescope/desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --debug",
    "bundle": "tauri build",
    "lint": "echo \"(todo)\"",
    "test": "echo \"(todo)\""
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
